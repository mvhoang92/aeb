# Detailed Technical Documentation

These subsystem documents provide implementation depth after the canonical
reading path in `../INDEX.md`. The canonical architecture/workspace/evaluation
contracts take precedence when an old command example conflicts with the
post-cleanup layout.

Recommended order: introduction → architecture → sensors → radar → camera/YOLO
→ fusion → braking → scenarios → dataset/training → run guide. Workflow and
report-format documents are maintainer references.

Historical experiment results belong in `../log/`; legacy documents are in
`../history/legacy_docs/`. Do not infer current scientific claims from a stale
example path—use paper-v5 claim mapping and frozen final evidence.
