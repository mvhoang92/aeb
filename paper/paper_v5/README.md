# Paper v5

Reviewer-driven revision of paper v4. The frozen 2,461-run campaign is unchanged;
v5 changes the statistical unit, derives severity metrics from raw tick logs,
clarifies scoring/sensor/dataset methods, expands related work and restores full
English/Vietnamese structural parity.

Build:

```bash
./build.sh
```

The build fails unless the English master is exactly six pages. See
`REVIEW_RESPONSE.md`, `CLAIM_EVIDENCE_MATRIX.md` and `SOURCE_MAP.md` before
editing claims.
